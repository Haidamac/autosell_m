<%= form_with(model: my_car) do |form| %>
  <% if my_car.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(my_car.errors.count, "error") %> prohibited this my_car from being saved:</h2>

      <ul>
        <% my_car.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
